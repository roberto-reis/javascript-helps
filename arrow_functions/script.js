/* function somar(x, y) {
    return x + y;
}
 */

/* let somar = function(x, y) {
    return x + y;
} */

/* let somar = (x, y) => {
    return x + y;
} */

let somar = (x, y) => x + y;

console.log( somar(8, 8) );